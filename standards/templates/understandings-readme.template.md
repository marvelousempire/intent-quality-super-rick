<!-- STATIONERY · understandings/ README — the folder's front desk
WHAT: the one-table manifest of every doc in an understandings/ folder — each doc's ROLE in one
line, so a new reader knows where to start and what each file is for.
FILENAME: README.md inside the understandings/ folder. Undated, updated the same commit any doc
is added/renamed (a stale manifest is a violation).
-->

# {System} understandings

Canonical home of the {system} understanding docs. <!-- ✍️ + the submodule/vault mount path if
this folder is mirrored elsewhere (e.g. Nephew/Understandings/{X}/), so readers know which copy
is source of truth. -->

| File | Role |
|------|------|
| `{System}-Ledger.md` | **Start here** — the one scorecard: vision vs reality, config, plans, gaps |
| `{SYSTEM} — COMP.md` | **North star** — the final master copy everything measures against |
| `{System}-Why-Guide.md` | **WHY table** — every tech + why it is best ({system} deck) |
| `{SYSTEM} — GAP ANALYSIS.md` | **Living crosswalk** — shipped vs COMP, conflicts, ranked gaps |
| `{SYSTEM} — BRIDGE PLAN.md` | **Execution** — the phased path from shipped to COMP |
| <!-- ✍️ …one row per doc. Role cell: bold the 1–2 word type keyword, then a dash + one-line hook.
     Keep pipeline order: index → north star → why → gap → bridge → audits → architectures →
     inventories → setup → rankings → benchmarks → incidents → patterns. --> | |

**Repo hub:** <!-- ✍️ link --> · **Forge:** <!-- ✍️ ssh://… origin -->
